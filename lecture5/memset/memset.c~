#include <stdio.h>
#include <stdlib.h>

int main() {

  char quote[] = "To be or not to be";

  printf("%s\n", quote);
  
  memset(quote, '.', 9);

  printf("%s\n", quote);

  free(quote);
  
  return EXIT_SUCCESS;

}
